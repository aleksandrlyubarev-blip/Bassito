# Telegram bot
python-telegram-bot==21.4

# Google Drive upload (Service Account)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Environment
python-dotenv>=1.0.0

# Process monitoring (CTA5 health monitor)
psutil>=5.9.0

# LongLive-2.0 HTTP service (FastAPI + SSE)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0
pydantic>=2.6.0
httpx>=0.27.0

# LongLive-2.0 inference (Blackwell only — NVFP4).
# Uncomment + install on the Blackwell engine node:
# torch>=2.6.0
# transformer-engine>=1.10.0
# nvidia-modelopt>=0.15.0
# diffusers>=0.27.0

# Local M5 image-to-video (Apple Silicon only — Ideogram 4 -> I2V).
# Uncomment + install on a MacBook Pro M5 Pro (macOS arm64):
# mlx>=0.18.0            # LTX-Video native runtime + Ideogram 4 nf4 sampling
# mlx-audio>=0.1.0       # optional local audio for LTX Desktop-style flows
# torch>=2.6.0           # MPS fallback for Wan 2.1 / HunyuanVideo backends
# diffusers>=0.27.0

# UI automation (only needed for CTA5 Strategy C — last resort)
# pyautogui>=0.9.54
# pywinauto>=0.6.8

# Job search module (/find_boost)
anthropic>=0.40.0
selectolax>=0.3.21
aiosqlite>=0.20.0
pypdf>=4.3.0
apscheduler>=3.10.4
PyYAML>=6.0
